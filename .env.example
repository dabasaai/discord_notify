# Discord Bot Token（Bot 需具備 Send Messages、Embed Links 權限）
DISCORD_BOT_TOKEN=

# 內部 API Key，呼叫方需在 x-api-key header 帶入
API_KEY=

# 服務監聽 port（容器內），對外由 frpc 轉發
PORT=3020

# channels.json 路徑（容器內絕對路徑）
CHANNELS_FILE=/app/config/channels.json

# 可選：Bot 斷線時呼叫的告警指令，例如 "tg-notify"。留空則只寫 log
ALERT_COMMAND=
